# Bugs & TODO

## 🐛 Баги

### Редактирование заметок (видимости) не работает
**Статус:** Не реализовано  
**Описание:** В `/notes` показываем кнопку `/edit_123` но хендлер не реализован  
**Файлы:** 
- `bot/handlers/commands.py` — показываем `/edit_{note['id']}`
- `bot/handlers/` — нет `edit.py`
- `core/memory.py` — функция `update_note_visibility()` готова, но не используется

**Что нужно:**
1. Создать `bot/handlers/edit.py` с хендлером для редактирования
2. Добавить ConversationHandler для выбора новой видимости
3. Вызвать `update_note_visibility()` из core/memory.py

---

## ✅ TODO для v2.1

- [ ] Реализовать редактирование видимости заметок
- [ ] Фильтрация заметок по тегам
- [ ] Поиск в заметках
- [ ] Напоминания (cron)
- [ ] Экспорт памяти (JSON/CSV)

---

## 🔄 Будущие версии (v3+)

- [ ] PostgreSQL вместо SQLite (для масштабирования)
- [ ] MCP-сервер (переиспользует core/)
- [ ] Веб-интерфейс
- [ ] Интеграция с календарём (Google Calendar)
- [ ] Синхронизация между девайсами
